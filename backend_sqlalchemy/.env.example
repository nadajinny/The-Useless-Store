PORT=8080
# 로컬에서는 SQLite가 기본이므로 설정 불필요. Postgres 쓰려면 아래처럼 설정하세요.
# DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/useless_store
JWT_SECRET=change_me_in_prod
CLIENT_ORIGIN=http://localhost:5173
